git clone https://github.com/eigencore/TinyRB.git

mv edu_fineweb10B.tar.gz TinyBR

cd TinyBR

tar -xzvf edu_fineweb10B.tar.gz

python -m venv env

source env/bin/activate

pip install -r requirements.txt

torchrun --standalone --nproc_per_node=8 train.py

tar -czvf log.tar.gz log

scp -P 9775 root@35.238.91.144:/workspace/TinyRB/log.tar.gz . 